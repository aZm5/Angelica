1.右键gitcode -->ls-->pwd-->clear
2.基本信息设置：git config --global user.name 'aZm5'
                       git config --global user.name '695231279@qq.com'
3. 提交文件：
   新建文件夹：mkdir gitTest--> cd gitTest/ -->git init
   进入gitTest 右键Git bash here-->git init
    touch a1.py-->a1.py-->git status (证明一下，你没有提交，你删了在这边是看不到你的改动的)
     touch a1.py
     git status(查看文件状态，此时文件在缓冲区，文件名是红的)
      git add a1.py（上传文件到暂存区）
     git status(再查看文件状态，文件名变绿了）
      git commit -m 'add a1.py'（提交文件）
      再查看一下状态
 4.修改仓库文件：
      ls-->查看文件状态-->vi 文件名（修改文件内容）-->按i键，就可以上下左右移动修改内容-->按Esc键，然后输:wq退出-->cat a1.py(打印文件内容)
      再查看状态（又红了）--》按照以上方法再提交一次（提交的时候引号中不需要写文件名了，可以写上备注信息）
5.上传文件夹:
    git config --global user.name 'aZm5'
    git config --global user.email  '695231279@qq.com'
     git add .
     git commit -m "wjj"
     git push(如果不可以转下一行)
     git remote add origin https://github.com/aZm5/Angelica.git，再git push
     如果两次在不同的机器上提交，会出现  [rejected] master -> master (fetch first)  提示先fetch（full）再merge(push)-->git pull在git push(git push --set-upstream origin master)
    github中的README.md文件不在本地代码目录中  :  提示master -> master (non-fast-forward)-->解决:git pull --rebase origin master 在git push(git push --set-upstream origin master)
     remote orign already exists. ----》 git remote rm origin



5.删除仓库文件
    rm -rf a1.py（删除本地的文件名）
    git rm a1.py（删除仓库中的文件名）
    git commit -m '删除1' 
    git push
6.删除文件夹：
    git rm -r 文件夹名（删除仓库中的文件名）
    git commit -m '删除1' 
    git push
